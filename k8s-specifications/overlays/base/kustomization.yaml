apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# FIX: Bỏ block "images:" cũ vì "name: visign-ai" không match
# image full path trong deployment (visignacrftll4r7kkywwy.azurecr.io/visign-ai).
# Việc override image sẽ làm ở overlay dev/prod thông qua kustomize images:
# với name = full ACR path.

resources:
  - namespace.yaml
  - visign-ai-deployment.yaml
  - visign-ai-service.yaml
  - visign-web-deployment.yaml
  - visign-web-service.yaml
