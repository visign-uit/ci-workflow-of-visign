apiVersion: v1
kind: Service
metadata:
  name: visign-web
  namespace: visign
  labels:
    app: visign-web
spec:
  selector:
    app: visign-web
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  type: ClusterIP